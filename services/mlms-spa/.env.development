# Proxy em vite.config: /api → BFF (ver docs/api/openapi-v0.yaml). Requer mlms-api em dev (ex.: :3000).
VITE_MLMS_API_BASE=/api/v1
# WebSocket do BFF Node (quando existir); vazio = canal desligado no UI
VITE_REALTIME_WS_URL=
